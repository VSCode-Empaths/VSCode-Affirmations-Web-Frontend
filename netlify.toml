# Serve from `public/` after `npm run build`. Fixes empty deploy when Netlify’s UI expects
# “Publish directory” = public (common default) while static files lived only at repo root.
[build]
  command = "npm run build"
  publish = "public"
